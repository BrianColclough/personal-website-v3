---
import { ClientRouter } from "astro:transitions";
import Footer from "../components/footer/footer.astro";
import NavBar from "../components/nav-bar";
import SEOHead from "../components/SEOHead.astro";
import "../styles/design-system.css";

interface Props {
    title: string;
    description?: string;
    image?: string;
    article?: boolean;
}

const {
    title,
    description = "Brian Colclough's personal website featuring photography, software engineering, and blog posts about AI and technology.",
    image,
    article,
} = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <SEOHead
            title={title}
            description={description}
            image={image}
            article={article}
        />
        <link rel="sitemap" href="/sitemap-index.xml" />
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Epilogue:ital,wght@0,100..900;1,100..900&family=Manrope:wght@200..800&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">
        <ClientRouter />
    </head>
    <body class="flex flex-col min-h-screen">
        <NavBar transition:persist client:load />
        <main id="main" class="flex-grow mt-16">
            <slot />
        </main>
        <Footer />
    </body>
</html>
